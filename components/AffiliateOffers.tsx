import AffiliateLink from "./AffiliateLink";
import { offersForArticle } from "@/lib/affiliates";
export default function AffiliateOffers({slug}:{slug:string}) {const offers=offersForArticle(slug); if(!offers.length) return null; return <section aria-labelledby="ofertas-titulo" className="mt-10 rounded-2xl border border-warm-300 bg-warm-50 p-6"><h2 id="ofertas-titulo" className="font-display text-2xl">Ofertas para comparar</h2><p className="mt-2 text-sm leading-relaxed">Links de afiliado: podemos receber comissão se você comprar. Confira preço, disponibilidade e condições na loja. A inclusão não significa teste prático ou recomendação médica.</p><div className="mt-5 grid gap-5 sm:grid-cols-2">{offers.map(offer => <div key={offer.id}><h3 className="font-semibold">{offer.title}</h3><p className="mt-2 text-sm">{offer.note}</p><AffiliateLink offer={offer}/></div>)}</div></section>;}
